[package]
name = "ros-package-manifest"
version = "0.1.0"
edition = "2021"

[dependencies]
quick-xml = { version = "0.36.1", features = ["overlapped-lists"] }
serde = { version = "1.0.209", features = ["derive"] }
