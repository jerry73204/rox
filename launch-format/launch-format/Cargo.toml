[package]
name = "launch-format"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
eyre.workspace = true
launch-types-common = { version = "0.1.0", path = "../launch-types-common" }
launch-types-xml = { version = "0.1.0", path = "../launch-types-xml" }
launch-types-yaml = { version = "0.1.0", path = "../launch-types-yaml" }
quick-xml = { version = "0.36.1", features = ["serialize"] }
serde_yaml = "0.9.34"
